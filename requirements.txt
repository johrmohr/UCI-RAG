# Streamlit Cloud Requirements - Minimal for Demo
# Python 3.11 compatible

streamlit==1.29.0
pandas==2.2.3
numpy==1.26.4
plotly==5.18.0
python-dotenv==1.0.0
requests==2.31.0

# Note: Heavy ML dependencies (sentence-transformers, torch, chromadb)
# are optional and only needed for full functionality
# The app will run in demo mode without them